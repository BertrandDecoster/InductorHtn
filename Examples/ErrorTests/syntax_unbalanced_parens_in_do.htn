% ERROR: Unbalanced parentheses in do() clause - extra closing paren
% Expected: Syntax error about unbalanced parentheses

travel(?dest) :- if(at(?here)), do(walk(?here, ?dest))).
walk(?from, ?to) :- del(at(?from)), add(at(?to)).
at(home).
