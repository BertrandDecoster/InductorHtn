% ERROR: Method fly-to() is defined but never called
% Expected: Warning about dead/unreachable method

travel(?dest) :- if(at(?here)), do(walk(?here, ?dest)).
fly-to(?dest) :- if(at(?here), has-wings), do(fly(?here, ?dest)).
walk(?from, ?to) :- del(at(?from)), add(at(?to)).
fly(?from, ?to) :- del(at(?from)), add(at(?to)).
at(home).
