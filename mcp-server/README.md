# InductorHTN MCP Server

Full documentation has moved to [`docs/tools/mcp-server.md`](../docs/tools/mcp-server.md).

Quick start:
```bash
cd mcp-server && python -m pytest tests/ -v   # run tests
python -m indhtn_mcp.server                    # run the server
```
